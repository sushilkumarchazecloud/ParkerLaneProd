({
	scrollTop: function (component, event){
        var scrollOptions = {
            left: 0,
            top: 300,
            behavior: 'smooth'
        }
        window.scrollTo(scrollOptions);
    },
})